name: New Relic Fluent Bit Output Plugin - Merge to master

on:
  push:
    branches:
      - NR-517619-rhel8-support

permissions:
  contents: write

jobs:
  cd:
    name: Continuous Delivery pipeline
    # Using Ubuntu 24.04 as runner, builds happen in Amazon Linux 2 Docker container
    # Amazon Linux 2 has GLIBC 2.26, which works on both AL2 (2.26) and RHEL8 (2.28)
    # Supported until June 2025, covers the oldest target system (Amazon Linux 2)
    runs-on: ubuntu-24.04
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
      version: ${{ steps.set_version.outputs.version }}

    steps:
      - name: Set up Go 1.23.6
        uses: actions/setup-go@v5 # Updated to v5 for better caching
        with:
          go-version: '1.23.6'

      - name: Check go version
        run: go version

      - name: Checkout code
        uses: actions/checkout@v4 # Updated to v4

      - name: Determine new plugin version
        id: set_version
        run: |
          VERSION=$(cat version.go | grep VERSION | awk '{gsub(/"/, "", $4); print $4}')
          echo "Determined version: $VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV       
      
      - name: Set up Zig
        uses: mlugg/setup-zig@v1
        with:
          version: 0.13.0

      - name: Build All Binaries (Zig)
        run: |
          echo "Starting Cross-Compilation with Zig..."
          
          # Array of targets: "GOOS GOARCH ZIG_TARGET OUTPUT_PREFIX EXTENSION"
          # We use glibc 2.26 for Linux to ensure compatibility with Amazon Linux 2
          targets=(
            "linux amd64 x86_64-linux-gnu.2.26 out_newrelic-linux-amd64 .so"
            "linux arm64 aarch64-linux-gnu.2.26 out_newrelic-linux-arm64 .so"
            "linux arm arm-linux-gnueabihf.2.26 out_newrelic-linux-arm .so"
            "windows amd64 x86_64-windows-gnu out_newrelic-windows-amd64 .dll"
            "windows 386 x86-windows-gnu out_newrelic-windows-386 .dll"
          )

          for target in "${targets[@]}"; do
            read -r goos goarch zigtarget prefix ext <<< "$target"
            echo "Building for $goos/$goarch (Target: $zigtarget)..."
            
            CGO_ENABLED=1 GOOS=$goos GOARCH=$goarch \
            CC="zig cc -target $zigtarget" \
            CXX="zig c++ -target $zigtarget" \
            go build \
              -buildmode=c-shared \
              -ldflags="-s -w" \
              -o "${prefix}-${{ env.VERSION }}${ext}" \
              .
          done
          
          echo "Build complete. Artifacts:"
          ls -lh out_newrelic-*       

      # The QEMU will include more platforms such as arm64 to docker buildx builder
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3 # Updated to v3

      # The Docker Buildx builder will be used later to leverage from the cache while building the image
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3 # Updated to v3

      # Cache to be used by Docker Buildx
      - name: Set up Docker Buildx's cache
        uses: actions/cache@v4 # Updated to v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: ${{ runner.os }}-buildx-

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3 # Updated to v3
      #   with:
      #     username: ${{ secrets.SVC_DOCKER_HUB_USERNAME }}
      #     password: ${{ secrets.SVC_DOCKER_HUB_ACCESS_TOKEN }}

      # - name: Build and Publish Docker image
      #   if: ${{ !contains(env.VERSION, 'beta') }}
      #   uses: docker/build-push-action@v6 # Updated to v6
      #   env:
      #     DOCKERHUB_REPOSITORY: newrelic/newrelic-fluentbit-output
      #     IMAGE_TAG: ${{ env.VERSION }}
      #   with:
      #     context: ./
      #     file: ./Dockerfile
      #     push: true
      #     tags: |
      #       ${{ env.DOCKERHUB_REPOSITORY }}:latest
      #       ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }}
      #     builder: ${{ steps.buildx.outputs.name }}
      #     platforms: linux/amd64, linux/arm64, linux/arm/v7
      #     cache-from: type=local,src=/tmp/.buildx-cache
      #     cache-to: type=local,dest=/tmp/.buildx-cache

      # - name: Build and Publish Docker beta image
      #   if: ${{ contains(env.VERSION, 'beta') }}
      #   uses: docker/build-push-action@v6
      #   env:
      #     DOCKERHUB_REPOSITORY: newrelic/newrelic-fluentbit-output
      #     IMAGE_TAG: ${{ env.VERSION }}
      #   with:
      #     context: ./
      #     file: ./Dockerfile
      #     push: true
      #     tags: |
      #       ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }}
      #     builder: ${{ steps.buildx.outputs.name }}
      #     platforms: linux/amd64, linux/arm64, linux/arm/v7
      #     cache-from: type=local,src=/tmp/.buildx-cache
      #     cache-to: type=local,dest=/tmp/.buildx-cache
          
      # - name: Inspect published Docker image
      #   run: docker buildx imagetools inspect newrelic/newrelic-fluentbit-output:${{ env.VERSION }}

      # - name: Build and Publish Docker debug image
      #   uses: docker/build-push-action@v6
      #   env:
      #     DOCKERHUB_REPOSITORY: newrelic/newrelic-fluentbit-output
      #     IMAGE_TAG: ${{ env.VERSION }}-debug
      #   with:
      #     context: ./
      #     file: ./Dockerfile_debug
      #     push: true
      #     tags: |
      #       ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }}
      #     builder: ${{ steps.buildx.outputs.name }}
      #     platforms: linux/amd64, linux/arm64
      #     cache-from: type=local,src=/tmp/.buildx-cache
      #     cache-to: type=local,dest=/tmp/.buildx-cache

      # - name: Inspect published Docker debug image
      #   run: docker buildx imagetools inspect newrelic/newrelic-fluentbit-output:${{ env.VERSION }}-debug

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4 # Updated to v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-2

      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2 # Updated to v2

      # - name: Build and Publish Docker image for Firelens
      #   uses: docker/build-push-action@v6
      #   env:
      #     ECR_REGISTRY: 533243300146.dkr.ecr.us-east-2.amazonaws.com
      #     ECR_REPOSITORY: newrelic/logging-firelens-fluentbit
      #     IMAGE_TAG: ${{ env.VERSION }}
      #   with:
      #     context: ./
      #     file: ./Dockerfile_firelens
      #     push: true
      #     tags: |
      #       ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:latest
      #       ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
      #     builder: ${{ steps.buildx.outputs.name }}
      #     # Firelens image only available for amd64 and arm64 architectures
      #     platforms: linux/amd64, linux/arm64
      #     cache-from: type=local,src=/tmp/.buildx-cache
      #     cache-to: type=local,dest=/tmp/.buildx-cache

      # - name: Inspect published Docker Firelens image
      #   run: docker buildx imagetools inspect 533243300146.dkr.ecr.us-east-2.amazonaws.com/newrelic/logging-firelens-fluentbit:${{ env.VERSION }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ env.VERSION }}
          release_name: newrelic-fluent-bit-output-${{ env.VERSION }}
          draft: true
          prerelease: false

      - name: Include linux-amd64 artifact in release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./out_newrelic-linux-amd64-${{ env.VERSION }}.so
          asset_name: out_newrelic-linux-amd64-${{ env.VERSION }}.so
          asset_content_type: application/octet-stream

  build-windows:
    name: Build Windows binaries
    needs: cd  # Wait for main job to create release first
    # Windows cross-compilation on Ubuntu with mingw
    runs-on: ubuntu-24.04
    env:
      VERSION: ${{ needs.cd.outputs.version }}

    steps:
      - name: Set up Go 1.25.6
        uses: actions/setup-go@93397bea11091df50f3d7e59dc26a7711a8bcfbe # v4
        with:
          go-version: '1.25.6'

      - name: Checkout code
        uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2

      - name: Install mingw cross-compilers
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64 gcc-mingw-w64-i686 g++-mingw-w64-i686

      - name: Build project (Windows amd64)
        run: env make windows/amd64

      - name: Build project (Windows 386)
        run: env make windows/386

      - name: Include windows-amd64 artifact in release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.cd.outputs.upload_url }}
          asset_path: ./out_newrelic-windows-amd64-${{ needs.cd.outputs.version }}.dll
          asset_name: out_newrelic-windows-amd64-${{ needs.cd.outputs.version }}.dll
          asset_content_type: application/octet-stream

      - name: Include windows-386 artifact in release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.cd.outputs.upload_url }}
          asset_path: ./out_newrelic-windows-386-${{ needs.cd.outputs.version }}.dll
          asset_name: out_newrelic-windows-386-${{ needs.cd.outputs.version }}.dll
          asset_content_type: application/octet-stream

  build-arm:
    name: Build ARM binaries 
    needs: cd  # Wait for main job to create release first
    # ARM builds use QEMU emulation to build natively in older containers:
    # - arm64: Amazon Linux 2 (GLIBC 2.26) - compatible with AL2, RHEL8, CentOS 7/8, Debian Bullseye
    # - arm/v7: Debian Buster (GLIBC 2.28) - compatible with RHEL8 and newer
    runs-on: ubuntu-24.04
    env:
      VERSION: ${{ needs.cd.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2

      # Set up QEMU for arm64/arm emulation
      - name: Set up QEMU
        uses: docker/setup-qemu-action@27d0a4f181a40b142cce983c5393082c365d1480 # v1

      - name: Build ARM binaries in native containers
        run: ./.github/scripts/build-linux-arm.sh "${{ github.workspace }}" "${{ env.VERSION }}" "1.25.6"

      - name: Verify build artifacts
        run: |
          ls -lh out_newrelic-linux-arm64-${{ env.VERSION }}.so
          ls -lh out_newrelic-linux-arm-${{ env.VERSION }}.so

      - name: Include linux-arm64 artifact in release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.cd.outputs.upload_url }}
          asset_path: ./out_newrelic-linux-arm64-${{ needs.cd.outputs.version }}.so
          asset_name: out_newrelic-linux-arm64-${{ needs.cd.outputs.version }}.so
          asset_content_type: application/octet-stream

      - name: Include linux-arm artifact in release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.cd.outputs.upload_url }}
          asset_path: ./out_newrelic-linux-arm-${{ needs.cd.outputs.version }}.so
          asset_name: out_newrelic-linux-arm-${{ needs.cd.outputs.version }}.so
          asset_content_type: application/octet-stream

#   windows-docker-images:
#     name:  ${{ matrix.name }} Docker image generation and publishing
#     # We aim to support https://kubernetes.io/docs/setup/production-environment/windows/intro-windows-in-kubernetes/#windows-os-version-support
#     # More info: https://cloud.google.com/kubernetes-engine/docs/how-to/creating-a-cluster-windows#choose_your_windows_server_node_image
#     # Tag reference: https://hub.docker.com/_/microsoft-windows-servercore
#     # Compatibility matrix: https://docs.microsoft.com/en-us/virtualization/windowscontainers/deploy-containers/version-compatibility
#     strategy:
#       matrix:
#         include:
# #          - name: Windows Server 2019 (LTSC)
# #            windowsVersion: 2019
# #            windowsImageTag: ltsc2019-amd64
# #            imageTagSuffix: windows-ltsc-2019
# #            # Uses self-hosted runner which has Hyper-V virtualization support, since windows-2019 GH runner was deprecated
# #            runsOn: windows-x64-8-core
# #            # More info on isolation: https://learn.microsoft.com/en-us/virtualization/windowscontainers/manage-containers/hyperv-container
# #            isolation: hyperv
#           - name: Windows Server 2022 (LTSC)
#             windowsVersion: 2022
#             windowsImageTag: ltsc2022-amd64
#             imageTagSuffix: windows-ltsc-2022
#             runsOn: windows-2022
#             isolation: process

#     runs-on: ${{ matrix.runsOn }}

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Determine new plugin version
#         run: |
#           $env:VERSION=(Select-String -Path version.go -Pattern 'const VERSION = "(.*)"').Matches.Groups[1].Value
#           echo "Determined version: $env:VERSION"
#           # After the following command, the VERSION variable is available via the "env" command in the GH action
#           Add-Content -Path $env:GITHUB_ENV -Value "VERSION=$env:VERSION"

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.SVC_DOCKER_HUB_USERNAME }}
#           password: ${{ secrets.SVC_DOCKER_HUB_ACCESS_TOKEN }}

#       - name: Build and Publish Docker image for ${{ matrix.name }}
#         env:
#           DOCKERHUB_REPOSITORY: newrelic/newrelic-fluentbit-output
#           IMAGE_TAG: ${{ env.VERSION }}-${{ matrix.imageTagSuffix }}
#         run: |
#           docker build --platform windows/amd64 --isolation=${{ matrix.isolation }} -f Dockerfile.windows -t ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }} --build-arg WINDOWS_VERSION=${{ matrix.windowsVersion }} .
#           docker push ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }}

  # windows-docker-images-older-version:
  #   name:  ${{ matrix.name }} Docker image generation and publishing
  #   # Windows 2019 needs to run older version of fluent bit as base images are not being published after fluent bit 4.0.3
  #   # We aim to support https://kubernetes.io/docs/setup/production-environment/windows/intro-windows-in-kubernetes/#windows-os-version-support
  #   # More info: https://cloud.google.com/kubernetes-engine/docs/how-to/creating-a-cluster-windows#choose_your_windows_server_node_image
  #   # Tag reference: https://hub.docker.com/_/microsoft-windows-servercore
  #   # Compatibility matrix: https://docs.microsoft.com/en-us/virtualization/windowscontainers/deploy-containers/version-compatibility
  #   strategy:
  #     matrix:
  #       include:
  #         - name: Windows Server 2019 (LTSC)
  #           windowsVersion: 2019
  #           windowsImageTag: ltsc2019-amd64
  #           imageTagSuffix: windows-ltsc-2019
  #           # Uses self-hosted runner which has Hyper-V virtualization support, since windows-2019 GH runner was deprecated
  #           runsOn: windows-x64-8-core
  #           # More info on isolation: https://learn.microsoft.com/en-us/virtualization/windowscontainers/manage-containers/hyperv-container
  #           isolation: hyperv

  #   runs-on: ${{ matrix.runsOn }}

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Determine new plugin version
  #       run: |
  #         $env:VERSION=(Select-String -Path version.go -Pattern 'const VERSION = "(.*)"').Matches.Groups[1].Value
  #         echo "Determined version: $env:VERSION"
  #         # After the following command, the VERSION variable is available via the "env" command in the GH action
  #         Add-Content -Path $env:GITHUB_ENV -Value "VERSION=$env:VERSION"

  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.SVC_DOCKER_HUB_USERNAME }}
  #         password: ${{ secrets.SVC_DOCKER_HUB_ACCESS_TOKEN }}

  #     - name: Build and Publish Docker image for ${{ matrix.name }}
  #       env:
  #         DOCKERHUB_REPOSITORY: newrelic/newrelic-fluentbit-output
  #         IMAGE_TAG: ${{ env.VERSION }}-${{ matrix.imageTagSuffix }}
  #       run: |
  #         docker build --platform windows/amd64 --isolation=${{ matrix.isolation }} -f Dockerfile.windows_older -t ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }} --build-arg WINDOWS_VERSION=${{ matrix.windowsVersion }} .
  #         docker push ${{ env.DOCKERHUB_REPOSITORY }}:${{ env.IMAGE_TAG }}
